# This file is for demostration purpose to use by ansible script to generate dynamic YAML config for diff env

server:
  port: 50010


management:
  endpoints:
    web:
      exposure:
        include: ["health", "metrics"]
  server:
    port: 50011
  endpoint:
    health:
      show-details: always
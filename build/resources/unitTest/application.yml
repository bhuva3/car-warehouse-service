
server:
  port: 50010


management:
  endpoints:
    web:
      exposure:
        include: ["health", "metrics"]
  server:
    port: 50011
  endpoint:
    health:
      show-details: always



server:
  port: 50010


management:
  endpoints:
    web:
      exposure:
        include: ["health", "metrics"]
  server:
    port: 50011
  endpoint:
    health:
      show-details: always


spring:
    data:
      mongodb:
        uri: mongodb://warehousedbuser:password@127.0.0.1:27017/WarehouseDatastore?authSource=admin